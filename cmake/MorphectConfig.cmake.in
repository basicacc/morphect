@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

include("${CMAKE_CURRENT_LIST_DIR}/MorphectTargets.cmake")

check_required_components(Morphect)

# Provide some useful variables
set(MORPHECT_VERSION @PROJECT_VERSION@)
set(MORPHECT_INCLUDE_DIRS "@PACKAGE_INCLUDE_INSTALL_DIR@")
